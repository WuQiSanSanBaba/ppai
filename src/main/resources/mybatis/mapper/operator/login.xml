<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wuqisan.ppai.operator.user.mapper.LoginMapper">
    <!--查询-->
    <select id="getUserById" resultType="com.wuqisan.ppai.operator.user.bean.UserInfo">
        select id,
               name,
               class_id,
               username,
               password,
               phone,
               sex,
               id_number,
               status,
               create_time,
               update_time,
               create_user,
               update_user
        from user_info
        where id = #{id}
    </select>
    <select id="getUserByUsername" resultType="com.wuqisan.ppai.operator.user.bean.UserInfo">
        select id,
               name,
               class_id,
               username,
               password,
               phone,
               sex,
               id_number,
               status,
               create_time,
               update_time,
               create_user,
               update_user
        from user_info
        where username = #{username}
    </select>
    <select id="getUserByUsernameAndPassword" resultType="com.wuqisan.ppai.operator.user.bean.UserInfo"
            parameterType="com.wuqisan.ppai.operator.user.bean.UserInfo">
        select id,
               name,
               class_id,
               username,
               password,
               phone,
               sex,
               id_number,
               status,
               create_time,
               update_time,
               create_user,
               update_user
        from user_info
        where username = #{username}
          and password = #{password}
    </select>
</mapper>